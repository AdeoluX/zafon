services:
  - type: web
    name: zafon
    runtime: node  # Specify the Node.js version
    buildCommand: |
      npm install
      npm run build
    startCommand: npm run start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    autoDeploy: true
